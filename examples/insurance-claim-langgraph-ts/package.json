{
  "name": "insurance-claim-langgraph-ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LangGraph port of the insurance-claim-decision-agent (TypeScript): createAgent (LangChain v1 / LangGraph) with ChatAnthropic over the same deterministic engine. Ships without a Halo integration — the Halo LangGraph adapter attaches separately.",
  "scripts": {
    "initdb": "bash scripts/init_db.sh",
    "seed": "tsx db/seed.ts",
    "agent": "tsx src/agent.ts",
    "reviewer": "tsx src/reviewer-console.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "langchain": "^1.0.0",
    "@langchain/langgraph": "^1.0.0",
    "@langchain/anthropic": "^1.0.0",
    "@langchain/core": "^1.0.0",
    "pg": "^8.21.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
