{
  "name": "@halo-format/claude",
  "version": "0.0.0",
  "description": "Halo host adapter for the Claude Agent SDK: a PostToolUse encode hook plus in-process halo_walk / halo_fetch MCP tools. installHalo() wires it in one call.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@halo-format/halo": "workspace:*",
    "zod": "^4.0.0"
  },
  "peerDependencies": {
    "@anthropic-ai/claude-agent-sdk": ">=0.3.0"
  },
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.161",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "vitest": "^1.5.0"
  },
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/halo-format/halo" }
}
